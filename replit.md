# Surya Portfolio

This project is a static portfolio website built with vanilla HTML, CSS, and JavaScript.

## Run on Replit

The configured `Start application` workflow serves the `docs` folder on port 5000:

```bash
python3 -m http.server 5000 --directory docs
```

Open the Replit preview after starting the workflow.